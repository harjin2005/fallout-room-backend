services:
  - type: web
    name: fallout-room-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py makemigrations incident_response && python manage.py migrate --run-syncdb && python manage.py collectstatic --noinput"
    startCommand: "gunicorn falloutroom.wsgi_simple:simple_app --bind 0.0.0.0:$PORT"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: falloutroom.settings_production
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
